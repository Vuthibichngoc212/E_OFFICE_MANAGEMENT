import { Container, Typography } from '@mui/material';

const Header = () => {
	return (
		<Container>
			<Typography variant={'h1'}>Đây là sidebar</Typography>
		</Container>
	);
};

export default Header;
